<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.base.app.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.base.app.dto.UserDto">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByEmail" resultMap="userResultMap">
        SELECT id, email, password, nickname, profile_image_url, role, status, created_at
        FROM blog_user
        WHERE email = #{email}
          AND deleted_at IS NULL
    </select>

    <select id="findById" resultMap="userResultMap">
        SELECT id, email, password, nickname, profile_image_url, role, status, created_at
        FROM blog_user
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM blog_user
        WHERE email = #{email}
          AND deleted_at IS NULL
    </select>

    <insert id="insert" parameterType="com.base.app.dto.UserDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO blog_user (email, password, nickname, role, status)
        VALUES (#{email}, #{password}, #{nickname}, 'USER', 'ACTIVE')
    </insert>

    <select id="findAll" resultMap="userResultMap">
        SELECT id, email, nickname, profile_image_url, role, status, created_at
        FROM blog_user
        WHERE deleted_at IS NULL
        ORDER BY id DESC
    </select>

    <update id="updateStatus">
        UPDATE blog_user SET status = #{status} WHERE id = #{id} AND deleted_at IS NULL
    </update>

    <update id="updateRole">
        UPDATE blog_user SET role = #{role} WHERE id = #{id} AND deleted_at IS NULL
    </update>

</mapper>
