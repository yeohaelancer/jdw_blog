<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.base.app.mapper.CategoryMapper">

    <resultMap id="categoryResultMap" type="com.base.app.dto.CategoryDto">
        <id property="id" column="id"/>
        <result property="blogId" column="blog_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="postCount" column="post_count"/>
    </resultMap>

    <select id="findByBlogId" resultMap="categoryResultMap">
        SELECT id, blog_id, parent_id, name, sort_order, post_count
        FROM blog_category
        WHERE blog_id = #{blogId} AND deleted_at IS NULL
        ORDER BY parent_id IS NULL DESC, sort_order ASC
    </select>

    <select id="findById" resultMap="categoryResultMap">
        SELECT id, blog_id, parent_id, name, sort_order, post_count
        FROM blog_category
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByBlogIdAndName" resultMap="categoryResultMap">
        SELECT id, blog_id, parent_id, name, sort_order, post_count
        FROM blog_category
        WHERE blog_id = #{blogId} AND name = #{name} AND deleted_at IS NULL
        LIMIT 1
    </select>

    <update id="adjustPostCount">
        UPDATE blog_category SET post_count = GREATEST(0, post_count + #{delta}) WHERE id = #{id}
    </update>

    <insert id="insert" parameterType="com.base.app.dto.CategoryDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO blog_category (blog_id, parent_id, name, sort_order)
        VALUES (#{blogId}, #{parentId}, #{name}, #{sortOrder})
    </insert>

    <update id="update" parameterType="com.base.app.dto.CategoryDto">
        UPDATE blog_category
        SET name = #{name}, parent_id = #{parentId}, sort_order = #{sortOrder}
        WHERE id = #{id} AND deleted_at IS NULL
    </update>

    <update id="softDelete">
        UPDATE blog_category SET deleted_at = NOW() WHERE id = #{id} AND deleted_at IS NULL
    </update>

</mapper>
